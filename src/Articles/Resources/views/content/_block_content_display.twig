{# This block is the by default block content display. The key anchor here is the twig {{ articleContent.content|raw }} #}

{% if null != articleContent.content %}
    <div class="bloc_content">
        <p>
            {{ articleContent.content|raw }}
        </p>
    </div>
{% else %}
    <div class="bloc_content_empty"></div>
{% endif %}
