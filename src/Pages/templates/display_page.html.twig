{% extends '@KazetennCore/blog_base.html.twig' %}

{% block title %}{{ page.title }}{% endblock %}

{% block body %}
    <div id="page_container" class="page_container">
        {% block page_title %}
            <div class="bloc_content">
                <h1 id="page_title" class="page_title">{{ page.title }}</h1>
            </div>
        {% endblock %}

        {# launch de blocks display  #}
        {% for order, data in page.blocksOrdered %}
            {{ include('@KazetennCore/content/_display_block.html.twig', {block: data}) }}
        {% endfor %}
    </div>
{% endblock %}
