{% extends '@Kazetenn/admin/admin_base.html.twig' %}

{% block title %}PageContent{% endblock %}

{% block body %}
    <h1>PageContent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ page_content.id }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ page_content.content }}</td>
            </tr>
            <tr>
                <th>Template</th>
                <td>{{ page_content.template }}</td>
            </tr>
            <tr>
                <th>Order</th>
                <td>{{ page_content.order }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('kazetenn_admin_page_content_index') }}">back to list</a>

    <a href="{{ path('kazetenn_admin_page_content_edit', {'id': page_content.id}) }}">edit</a>

    {{ include('@Kazetenn/admin/page_content/_delete_form.html.twig') }}
{% endblock %}
