<form method="post" action="{{ path('kazetenn_admin_page_content_delete', {'id': page_content.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ page_content.id) }}">
    <button class="btn">Delete</button>
</form>
